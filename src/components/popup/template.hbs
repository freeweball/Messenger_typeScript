<div class="popup__wrapper {{ classList }}">
    <div class="popup">
        {{#each childrens}}
            <div>{{{ this }}}</div>
        {{/each}}

        {{!-- <div class="popup__title">{{{ title }}}</div> --}}
        <div class="popup__text">{{{ text }}}</div>
        {{!-- <div class="popup__buttons">{{{ Button }}}</div> --}}
    </div>
</div>
